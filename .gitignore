# 환경 변수 / 비밀키 — 절대 커밋 금지
.env
*.key
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# 데이터 (샘플 소량만 data/samples/ 에 커밋 허용)
data/regulations/*
!data/regulations/.gitkeep
*.db
chroma/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code
.claude/worktrees/

# LLM 결과 캐시 (로컬 전용)
.cache/

# 데모 생성물(스크립트로 재현)
# data/samples/demo/ 는 커밋한다 — 가상 고객(김국민) 서류 4종, 총 277KB.
# 제출본을 받은 사람이 사전 판독 결과(data/demo_results/)로 시연하려면
# 우리가 해시한 바로 그 파일이 필요하다(재생성하면 바이트가 달라진다).

# 실물 검증용 PDF (다운로드물)
data/eval/

# 실제 고객 서류(개인정보 — 절대 커밋 금지)
data/realpkg/

# Office 임시 잠금 파일
~$*
