# 환경 변수 / 비밀키 — 절대 커밋 금지
.env
*.key
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# 데이터 (샘플 소량만 data/samples/ 에 커밋 허용)
data/regulations/*
!data/regulations/.gitkeep
# 조문 청크는 커밋한다(0.64MB) — 제출본에서 근거 조문 검색이 동작해야 한다.
# 원본 API 응답(*.raw.json, 1.6MB)은 재수집용이라 제외한다.
!data/regulations/*.articles.json
*.db
chroma/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code
.claude/worktrees/

# LLM 결과 캐시 (로컬 전용)
.cache/

# 데모 생성물(스크립트로 재현)
# data/samples/demo/ 는 커밋한다 — 가상 고객(김국민) 서류 4종, 총 277KB.
# 위반 3건을 심어둔 시연 시나리오라 제출본에 포함되어야 한다.

# 실물 검증용 PDF (다운로드물)
data/eval/

# 실제 고객 서류(개인정보 — 절대 커밋 금지)
data/realpkg/

# Office 임시 잠금 파일
~$*
