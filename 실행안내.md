# 실행 안내

**금융상품 판매서류 컴플라이언스 검증 AI Copilot** — 제8회 KB Future Finance A.I. Challenge

---

## 가장 빠른 방법 (Windows)

### **`run.bat` 을 더블클릭하세요.**

이것만 하면 됩니다. 파이썬 버전을 고르거나 명령어를 입력할 필요가 없습니다.

`run.bat` 이 알아서 처리하는 것:

1. 쓸 수 있는 파이썬을 찾습니다 (3.10 이상)
2. 이 폴더 안에 전용 실행 환경(`.venv`)을 만듭니다 — **PC에 이미 설치된 파이썬 환경은 건드리지 않습니다**
3. 필요한 패키지를 내려받습니다
4. 앱을 실행하고 브라우저를 엽니다

```
============================================================
 KB 판매서류 컴플라이언스 검증 Copilot
============================================================

[1/4] Python 3.12.10
[2/4] 가상환경을 만듭니다. 처음 한 번만 걸립니다.
[3/4] 필요한 패키지를 확인합니다. 처음에는 몇 분 걸릴 수 있습니다.
[4/4] 앱을 실행합니다. 브라우저가 자동으로 열립니다.

  Local URL: http://localhost:8501
```

> **처음 실행은 3~5분 걸립니다** (패키지 내려받기). 두 번째부터는 몇 초 만에 뜹니다.
> 검은 창이 멈춘 것처럼 보여도 정상이니 기다려 주세요.

브라우저가 자동으로 안 열리면 주소창에 **http://localhost:8501** 을 입력하세요.
종료하려면 검은 창에서 **Ctrl + C** 를 누릅니다.

---

## macOS · Linux

```bash
python3 scripts/launch.py
```

`run.bat` 과 똑같이 동작합니다.

---

## 요구 사항

| 항목 | 내용 |
|---|---|
| 파이썬 | **3.10 이상** (개발·검증은 3.12 기준) |
| 인터넷 | 최초 1회 필요 (패키지 설치·AI 판독) |
| API 키 | **`.env` 에 포함해 제출했습니다. 따로 발급하실 필요 없습니다.** |
| 그 외 | 없음 (DB·도커·별도 서버 불필요) |

파이썬이 없다면 https://www.python.org/downloads/ 에서 설치하시고,
설치 화면의 **"Add python.exe to PATH"** 를 체크해 주세요.

---

## 무엇을 해 보면 되는가

### 1. 위반이 있는 서류 (준비돼 있습니다)

앱이 뜨면 **`판매 건 1 — 서류 4종`** 칸에 `data/samples/demo/` 폴더의 **PDF 4개를 전부 선택해 한 번에** 올려 주세요.

```
data/samples/demo/
├─ 01_적합성진단표.pdf     고객 김국민 · 투자성향 "안정형"
├─ 02_상품설명서.pdf        KB 글로벌 하이일드 · 위험등급 "1등급"
├─ 03_가입신청서.pdf        계약일 2026-07-12
└─ 04_설명확인서.pdf        설명일 2026-07-15 · 고객 확인 "미서명"
```

**서류를 한 장씩 보면 넷 다 정상입니다.** 맞대어 보면 위반 3건이 드러납니다.

| 검사 | 판정 | 근거 |
|---|---|---|
| FIT-001 적합성 | 🔴 위험 | 안정형 고객에게 1등급 상품 → 금소법 **제17조** |
| DATE-001 선후관계 | 🔴 위험 | 설명일(07-15)이 계약일(07-12)보다 **늦음** → **제19조** |
| ACK-001 설명 확인 | 🔴 위험 | 고객 확인란 **미서명** → **제19조** |

종합 결론에 **"판매 진행 부적합 — 위반 소지 3건"** (통과 4 · 주의 1 · 누락 0 · 위험 3) 이 뜨면 정상 동작입니다.

> 계약을 여러 건 검증하시려면 **"판매 건 추가"** 로 칸을 늘리세요. 칸마다 따로 판정합니다.
> 서류를 한 칸에 섞으면 어느 계약 것인지 알 수 없어 잘못된 판정이 나올 수 있어서 이렇게 나눴습니다.

### 2. 정상 서류도 넣어 보세요

컴플라이언스 도구는 '다 잡아내는 것'보다 **'정상을 정상이라고 말하는 것'** 이 중요합니다.
실제 판매서류를 넣으면 **위험 0건**으로 통과합니다 (실물 25건 전수 감사 기준 오탐 0건).

### 3. 판정 근거를 확인하세요

- **검사 항목을 펼치면** 어느 서류의 어느 값 때문인지, 근거 법령 조문이 무엇인지 나옵니다
- **"원문 보기"** 를 누르면 법령 조문 전문을 그 자리에서 볼 수 있습니다
- 화면 아래 **"서류 원문 하이라이트"** 에서 판정 근거가 된 위치가 원본 PDF 위에 표시됩니다

---

## 이 도구의 설계 — 판정은 AI가 하지 않습니다

**AI는 읽기만 하고, 판정은 코드가 합니다.**

AI(LLM)는 서류 유형 분류와 값 추출에만 쓰이고, "위반인가 아닌가"는 전부 결정론적 파이썬 규칙이 판단합니다. 그래서 같은 서류·같은 정책이면 **언제나 같은 결과**가 나옵니다.

- 업로드 순서 24가지로 반복 → 판정 조합 **1가지**
- 회귀 테스트 45건 (아래 명령으로 직접 확인 가능)

```powershell
.venv\Scripts\python.exe -m pytest -q
```

---

## 잘 안 될 때

### `python` 이 없다고 나옵니다
파이썬 설치 시 **"Add python.exe to PATH"** 를 체크하지 않은 경우입니다.
파이썬을 다시 설치하거나, 설치 프로그램에서 **Modify → Add to PATH** 를 선택하세요.

### Microsoft Store 창이 열립니다
Windows의 가짜 `python` 바로가기가 실행된 것입니다. **`run.bat` 을 쓰시면** 이 문제를 피해 갑니다.

### `ModuleNotFoundError: No module named 'streamlit'`
`run.bat` 은 패키지를 이 폴더의 `.venv` 안에 설치합니다. 그래서 별도로 `python -m streamlit ...` 을 치면 찾지 못합니다.

**`run.bat` 을 다시 실행하시면 됩니다.** 직접 명령으로 실행하고 싶다면:

```powershell
.venv\Scripts\python.exe -m streamlit run app/main.py
```

### `py -3 -m streamlit run app/main.py` 로 실행했더니 이상합니다
`py -3` 은 **설치된 것 중 가장 오래된 3.9를 고르는 경우가 있습니다** (구버전 Windows 런처의 버전 비교 문제). 지원이 끝난 런타임이라 권장하지 않습니다. `run.bat` 을 쓰세요.

### 검은 창에 경고가 잔뜩 나오고 화면이 안 뜹니다
`python app/main.py` 로 실행하신 경우입니다. Streamlit 앱은 그 방식으로 실행되지 않습니다. `run.bat` 을 쓰세요.

### 포트 8501이 이미 사용 중이라고 합니다
다른 프로그램이 그 포트를 쓰고 있습니다.

```powershell
.venv\Scripts\python.exe -m streamlit run app/main.py --server.port 8600
```

### 판정이 "미확인"으로만 나옵니다
`.env` 파일이 없거나 API 키가 비어 있는 경우입니다. 제출본에 포함돼 있어야 하며, 없으면 앱이 **검증을 중단합니다** — 잘못된 판정을 내놓지 않기 위한 의도된 동작입니다.

---

## 폴더 구성

```
run.bat              ← 더블클릭하면 실행됩니다
실행안내.md          ← 이 문서
README.md            프로젝트 개요
.env                 API 키 (제출본에 포함)

app/main.py          검증 화면
src/ingest/          법령 수집 + 조문 검색
src/verify/          검증 규칙 8종 (결정론적 판정)
src/parser/          서류 판독 · 필드 추출 · 하이라이트 좌표
src/common/          공용 스키마
data/samples/demo/   시연용 서류 4종 (가상 고객)
data/regulations/    금융소비자보호법 조문
tests/               회귀 테스트 45건
docs/                기술설명서
```

---

## 개인정보 처리

업로드한 서류는 판독을 위해 Anthropic API로 전송되며 **모델 학습에 사용되지 않습니다.**
원본 파일은 디스크에 저장하지 않습니다. 재호출 비용을 줄이기 위해 판독 결과만 로컬 `.cache/llm` 에 남으며, 화면의 **"판독 캐시 비우기"** 버튼으로 지울 수 있습니다.
